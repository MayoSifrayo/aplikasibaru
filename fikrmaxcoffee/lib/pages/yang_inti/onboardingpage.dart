import 'package:flutter/material.dart';
class Onboardingpage extends StatelessWidget {
  
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Boarding Page')),
      body: Column(
        children: [],
        ),
    );
  }
}